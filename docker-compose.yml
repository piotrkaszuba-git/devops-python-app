version: '3.8'

services:
  app:
    build: ./app
    ports:
      - "8083:8000"
    depends_on:
      - db

  db:
    image: postgres
    environment:
      POSTGRES_USER: devops
      POSTGRES_PASSWORD: devops123
      POSTGRES_DB: myapp
